using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EquipmentActivate_scr : MonoBehaviour
{
    [SerializeField, Header("武器を入れる(順番注意)")] public GameObject _weapons;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
