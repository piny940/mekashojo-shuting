using UnityEngine;

namespace View
{/*
    public class CannonFire__Player : CollisionBase
    {
        // Start is called before the first frame update
        void Start()
        {
            playWhileIn += (Collider2D collision) =>
            {
                if (collision.tag == "BattleScenes/Enemy")
                {
                    Model.EnemyDamageManager enemyDamageManager = collision.GetComponent<Controller.EnemyDamageManager>().enemyDamageManager;

                    _cannonFire__Player.playerFire.DoDamage(enemyDamageManager,
                        Model.EquipmentData.equipmentData.equipmentStatus
                        [Model.EquipmentData.equipmentType.MainWeapon__Cannon]
                        [Model.EquipmentData.equipmentData.equipmentLevel[Model.EquipmentData.equipmentType.MainWeapon__Cannon]]
                        [Model.EquipmentData.equipmentParameter.Power] * Time.deltaTime);
                }
            };
        }
    }
    */
}
